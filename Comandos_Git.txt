git config --global user.name "karlos7"
git . (add tudo)
git nome do arquivo (add arquivo especifico)
Testando só com o commit -m (Tem que dar o push)
git commit -a -m (não precisa do add, já commita logo)

git diff (Testando visualizar o que eu modifiquei)